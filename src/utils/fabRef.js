// Holder del ref de la FloatingAction (speed-dial naranja de BottomTabs).
// Permite que el FAB del sidebar (AppShell) abra el mismo menú de acciones.
const fabRef = {current: null};
export default fabRef;
